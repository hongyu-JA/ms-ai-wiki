{
  "name": "ms-ai-wiki-automation",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Automation pipeline for the Microsoft AI Obsidian knowledge base (fetch → filter → patch → apply).",
  "scripts": {
    "fetch": "tsx scripts/fetch.ts",
    "filter": "tsx scripts/filter.ts",
    "patch": "tsx scripts/patch.ts",
    "apply": "tsx scripts/apply.ts",
    "sync": "npm run fetch && npm run filter && npm run patch && npm run apply",
    "sync:dry": "cross-env DRY_RUN=1 DRY_WRITE=1 npm run sync"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "fast-xml-parser": "^4.5.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "cross-env": "^7.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
